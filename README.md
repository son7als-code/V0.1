# Quy Thuat Video AI Core Repository

The V0.1 application lives in [`quy_thuat_video_core/`](quy_thuat_video_core/).

Run mock mode from that directory:

```bash
cd quy_thuat_video_core
python -m app.main --mock
```

Run tests from the repository root:

```bash
pytest
```
